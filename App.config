<?xml version="1.0" encoding="utf-8" ?>
<configuration>

  <!-- 
    String de conexão para o banco AppDev.
    Evite armazenar credenciais sensíveis no controle de versão.
    Prefira Integrated Security ou um gerenciador seguro de segredos.
  -->
  <connectionStrings>
    <!-- Exemplo usando autenticação integrada (Windows) -->
    <!--
    <add name="SqlConn" connectionString="Server=localhost;Database=AppDev;Integrated Security=True;" providerName="System.Data.SqlClient" />
    -->

    <!-- Se precisar usar autenticação SQL, armazene as credenciais de forma segura -->
    <add name="SqlConn" connectionString="Server=localhost;Database=AppDev;User Id=sistema_app;Password=SenhaForte@123;TrustServerCertificate=True;" providerName="System.Data.SqlClient" />
  </connectionStrings>
  
</configuration>